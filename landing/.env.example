# URL of the logged-in app (client/ + server/), for the login/signup CTAs.
NEXT_PUBLIC_APP_URL=http://localhost:3100

# Server-side fetch target for the public plans endpoint
# (server/routes/public-plans.js, mounted on the PUBLIC app — port 3104 in dev).
PLANS_API_URL=http://localhost:3104/api/public/plans
